apiVersion: v1
kind: Service
metadata:
  name: smartx-ledger
  labels:
    app: smartx-ledger
spec:
  type: ClusterIP
  selector:
    app: smartx-ledger
  ports:
    - name: http
      port: 8080
      targetPort: 8080
