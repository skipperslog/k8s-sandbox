image: python:3.12-slim

pipelines:
  default:
    - step:
        name: Test
        script:
          - cd smartx-ledger
          - pip install -r requirements.txt -r requirements-dev.txt
          - pytest -q
